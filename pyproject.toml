[project]
name = "ytmd"
version = "1.0.0"
description = "Telegram bot that turns YouTube Music links into audio files"
requires-python = ">=3.11"
dependencies = [
    "yt-dlp",
    "ytmusicapi",
    "requests",
    "mutagen",
]

[tool.vercel]
entrypoint = "app:app"
